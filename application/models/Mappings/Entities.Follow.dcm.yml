Entities\Follow:
  type: entity
  manyToOne:
    user:
      targetEntity: User
    follow:
      targetEntity: User
  table: follow
  fields:
    id:
      type: integer
      id: true
      generator:
        strategy: AUTO
